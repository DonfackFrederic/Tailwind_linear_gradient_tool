import { OptionToFullTextPipe } from './option-to-full-text.pipe';

describe('OptionToFullTextPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionToFullTextPipe();
    expect(pipe).toBeTruthy();
  });
});
